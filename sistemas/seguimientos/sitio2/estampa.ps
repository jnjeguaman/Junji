/MonthName
[
(01) (02) (03) (04)
(05) (06) (07) (08)
(09) (10) (11) (12)
] def

<<
/EndPage
{
2 eq { pop false }
{
gsave  
/Helvetica_Bold 10 selectfont

/strcat
{ 0 3 2 roll % <str> <pos> <A>
{
2 index length 2 index sub % <str> <pos> A[i] <freelen>
exch 4 copy pop getinterval % <str> <pos> <freelen> A[i] <free>
1 index type /stringtype ne
{ cvs }{ copy } ifelse % <str> <pos> <freelen> <buf>
length exch pop add % <str> <pos'>
} forall
0 exch getinterval
} bind def

{ [ (%Calendar%) currentdevparams
} stopped
{ cleartomark (no clock)
}{
begin
.60 setgray 50 665 moveto 45 rotate ()
Day (/) Month (/) Year (. )
Hour (:) Minute (:) Second ( )
end ]
50 string strcat
} ifelse

show
grestore
true
} ifelse
} bind
>> setpagedevice